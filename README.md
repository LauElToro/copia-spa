# copia-spa

Prototipo frontend Liberty Club (Next.js). Modo demo con datos mock: `NEXT_PUBLIC_FRONTEND_ONLY` (por defecto activo en build).

## Desarrollo

```bash
yarn install
yarn dev
```

Abrir [http://localhost:3000](http://localhost:3000).
